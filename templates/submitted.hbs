{{>html_header}}
<div class="wrapper contact">
    {{>header}}
    <div class="highlight padded box submitted">
        {{message}}
    </div>
    {{>footer}}
</div>
<script src="js/submitted.js"></script>
{{>html_footer}}